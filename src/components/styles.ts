import { SxProps } from '@mui/material';

export const gameSettingsStyles: SxProps = {
    display: 'flex',
    justifyContent: 'space-between',
    alignItems: 'center',
    width: '100%',
    margin: 1,
};
